(()=>{
'use strict';
const form=document.getElementById('setupForm'),msg=document.getElementById('setupMessage');
async function api(path,opt={}){const r=await fetch('../api/v1'+path,{method:opt.method||'GET',credentials:'same-origin',headers:{'Accept':'application/json',...(opt.body?{'Content-Type':'application/json'}:{})},body:opt.body?JSON.stringify(opt.body):undefined});let j={};try{j=await r.json()}catch{}if(!r.ok){const e=new Error(j?.error?.message||'Request failed.');e.code=j?.error?.code;throw e}return j.data}
async function init(){try{const s=await api('/admin/setup/status');if(s.configured){msg.textContent='تم إنشاء حساب الإدارة بالفعل. استخدم صفحة تسجيل الدخول.';msg.hidden=false;form.querySelector('button').disabled=true}else if(!s.setup_enabled){msg.textContent='الإعداد مقفول. تأكد من وجود ADMIN_SETUP_KEY و ADMIN_PASSWORD_PEPPER كـ Worker Secrets ثم أعد تحميل الصفحة.';msg.hidden=false}}catch(e){msg.textContent=e.message;msg.hidden=false}}
form.addEventListener('submit',async e=>{e.preventDefault();msg.hidden=true;const b=form.querySelector('button');b.disabled=true;const d=Object.fromEntries(new FormData(form));try{await api('/admin/setup',{method:'POST',body:d});location.replace('index.html#overview')}catch(err){const map={ADMIN_PASSWORD_INVALID:'كلمة المرور يجب أن تكون 12 حرفًا على الأقل.',ADMIN_EMAIL_INVALID:'أدخل بريدًا إلكترونيًا صحيحًا.',ADMIN_NAME_INVALID:'أدخل اسم مدير صحيحًا.',ADMIN_PASSWORD_PEPPER_MISSING:'سر حماية كلمة المرور غير مُعد على Cloudflare.'};msg.textContent=map[err.code]||err.message;msg.hidden=false;b.disabled=false}});init();
})();
