# NEXORA Phase 6.2 — تطوير طلبات العملاء والـLive Refresh

هذه المرحلة مبنية فوق **Phase 6.1.1 Auth Hotfix + Phase 6.1.2 Light Theme**.

## ما تم
- تعريب/ترجمة الحالات حسب لغة لوحة التحكم بدل عرض مفاتيح مثل `contacted` و`proposal_sent`.
- مرحلة المشروع والمدة ووضع الميزانية تظهر عربي في لوحة العربي وإنجليزي في لوحة الإنجليزي.
- الخدمات المطلوبة أصبحت Cards حديثة ومتناسقة داخل تفاصيل الطلب.
- وصف العميل يحترم اتجاه النص تلقائيًا `RTL/LTR` بدون تغيير كلام العميل.
- سجل الحالة أصبح Timeline أوضح مع ترجمة الحالات واسم من قام بالتغيير والتاريخ.
- إضافة ملاحظة اختيارية عند تغيير الحالة لتظهر في سجل الحالة.
- حفظ لغة العميل في الطلبات الجديدة داخل D1.
- تحديث تلقائي للطلبات أثناء فتح لوحة التحكم؛ لا تحتاج Refresh يدوي حتى يظهر الطلب الجديد.
- عند وصول طلب جديد تظهر رسالة داخل الـDashboard ويتم تحديث القائمة والعدادات.
- تصحيح حالة النظام داخل الـDashboard إلى Cloudflare Worker / D1 بدل PHP / MySQL.

## طريقة التحديث
1. فك Overlay فوق نفس فولدر المشروع الحالي عندك ووافق على Replace.
2. **لا تمسح D1 ولا Admin ولا Secrets.**
3. افتح PowerShell داخل المشروع:

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\scripts\APPLY_6_2_INQUIRY_UX.ps1
```

4. عند سؤال Wrangler عن Migration اختار `Y`.
5. بعد النشر افتح لوحة التحكم واعمل `Ctrl + F5` مرة واحدة.
