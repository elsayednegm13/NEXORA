{
  "name": "nexora-cloudflare-native",
  "version": "6.1.0",
  "private": true,
  "description": "NEXORA Technologies — Cloudflare Workers + D1 native deployment",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "type": "module"
}
